from docx import Document


def add_section_text(doc, level, title, text):
    doc.add_heading(title, level=level)
    paragraphs = [p.strip() for p in text.strip().split("\n\n") if p.strip()]
    for p in paragraphs:
        doc.add_paragraph(p)


def main():
    doc = Document()

    # 标题
    doc.add_heading("智能瞄准装置设计", level=0)
    doc.add_paragraph("（基于 OpenMV4 H7 + STM32 的视觉辅助双轴云台系统）")

    add_section_text(
        doc,
        1,
        "1 绪论",
        """
智能瞄准装置是将视觉感知、运动控制与人机交互融合的综合系统，广泛应用于机器人竞赛、智能安防、实验教学等场景。随着嵌入式处理能力和视觉算法的提升，小型化、低成本的智能瞄准系统逐渐成为本科工程实践的热点课题。

本课题以 OpenMV4 H7 视觉模块为感知核心，以 STM32F103 系列控制板为执行与控制核心，结合步进电机/舵机组成双轴云台，配合激光指示与 OLED 显示，实现“检测—定位—校正—提示”的完整工作链路。系统对成本、体积和实时性均提出较高要求，因此选择轻量串口通信与 DMA 接收方式，并在电机侧采用 PID 控制算法，实现稳定、快速的对准过程。

智能瞄准系统的关键在于“感知—决策—执行”闭环的高效协作。视觉端需要在有限算力下提取稳定的目标中心，控制端需要在短周期内完成误差计算与电机驱动，任何一端的延迟或波动都会影响整体对准精度，因此系统设计需要从数据流与控制链路整体统筹。

在小型嵌入式场景中，计算资源受限、供电噪声与执行机构非理想性普遍存在。为此，本设计在控制端采用稳健的 DMA 缓冲与帧校验机制，避免通信失真导致的误控制；在机械与控制方面设置软限位、回中与命中锁定策略，提高系统鲁棒性。

系统设计过程中还需考虑人机交互的友好性与可调试性。通过 OLED 实时显示坐标与状态，使用按键实现启动与重置，可有效提升操作便利性，并降低误操作风险。

在工程实现中，交互设计还应兼顾系统安全性。比如在未完成初始化或未检测到目标时，系统保持电机关闭状态，避免误触发导致机构运动，这一点在实际设备中尤为重要。

从工程实践角度看，智能瞄准装置具备模块化、可复用与可扩展特点。通过更换视觉算法、升级目标识别模型或加入弹道补偿模块，即可扩展到更复杂的场景。其架构与方法具有较强的通用性，可迁移到其他视觉伺服与自动跟随任务。

本章后续将从研究背景、国内外现状与论文结构三方面展开，为系统设计提供问题定义、技术路线与逻辑支撑。

需要说明的是，智能瞄准系统不仅关注静态对准精度，更关注动态过程的响应速度与稳定性。当目标移动、光照变化或机械摩擦发生变化时，系统需要保持可控性与鲁棒性。因而在设计中必须综合考虑视觉端的稳定输出、通信端的低延迟以及控制端的平滑驱动。

此外，系统的“观测—控制”链路存在不可避免的延迟，尤其是视觉帧率有限时，控制端获得的是上一帧的目标位置。为了降低延迟带来的超调风险，系统采用“快—慢切换”控制策略，并在误差小于阈值时减速，从而改善微小误差时的稳定性。

综上所述，绪论部分不仅阐明了研究背景与意义，也为后续章节的技术路线与实现细节提供理论依据。
""",
    )

    add_section_text(
        doc,
        2,
        "1.1 研究背景及意义",
        """
传统瞄准装置依赖人工调整或机械限位，存在效率低、精度不足、环境适应性差等问题。智能瞄准系统通过引入机器视觉与嵌入式控制，能够在复杂环境下完成目标识别、位置估计和自动跟踪，从而显著提高对准效率与稳定性。

在工业与安防领域，自动对准技术可用于视觉引导、监测与追踪等应用，减少人工干预并提升安全性；在机器人竞赛与教育教学领域，该技术是锻炼系统集成能力的重要载体，具备较高的教学价值与展示价值。

随着轻量化视觉硬件的发展，像 OpenMV 这类端侧视觉模块能够直接输出目标坐标，为微控制器提供“结构化数据”。这使得传统 MCU 也能实现复杂视觉控制任务，拓宽了嵌入式系统的应用边界。

从控制理论角度看，智能瞄准装置是典型的视觉伺服系统，包含传感器噪声、执行机构非线性与通信延迟等问题。对其进行研究不仅有助于理解 PID 控制与系统稳定性，还能帮助学生理解工程系统中的误差来源与补偿策略。

本课题以“低成本、易实现、可扩展”为目标，采用 STM32F103 + OpenMV4 H7 的组合方案，既保证了实时性与可控性，又便于后续功能扩展。对于本科毕业设计而言，该课题覆盖了软硬件协同设计的完整链路，具备较好的综合训练效果。

此外，本设计通过引入命中判定、矩形约束与回中策略，进一步提高系统鲁棒性，使其更接近实际工程系统的设计思路与规范。

从课程设计与毕业设计角度看，该课题具备较高的综合训练价值，涵盖嵌入式开发、机械结构、电子电路、控制理论与图像处理等内容，有助于学生形成完整的系统工程视角。

最后，智能瞄准系统的研究还可为后续科研与工程项目提供基础实验平台，例如引入多目标识别、目标分类与运动预测等功能，进一步拓展系统能力边界。
""",
    )

    add_section_text(
        doc,
        2,
        "1.2 国内外研究现状",
        """
国外在小型化视觉系统方面起步较早，Jetson、Raspberry Pi 等平台已支持复杂的深度学习识别，但在功耗、成本与实时性方面对嵌入式教学并不友好。近年来出现的 OpenMV 系列，通过集成 MCU 与摄像头，将图像处理下沉到端侧执行，适合用于轻量级视觉控制系统。

在视觉伺服领域，国外研究更多集中于模型预测控制、卡尔曼滤波与多传感器融合，提高动态目标跟踪精度与抗干扰能力。然而这些方法对算力与建模要求较高，通常运行在高性能平台上。

国内高校与工程实践普遍采用 STM32 与 OpenMV 的组合方案，并在云台控制、目标检测与通信稳定性方面形成较为成熟的工程案例。但多数作品仍存在控制模型粗糙、对丢帧缺乏鲁棒处理等问题，工程稳定性有待提升。

在执行机构方面，国内研究常采用步进电机+驱动模块的方式实现云台控制，优点是实现简单、定位精度高，但在高速响应与噪声控制方面仍有改进空间。一些研究开始引入舵机或无刷电机方案，提升动态性能。

随着嵌入式视觉算法的持续优化，目标检测开始从简单阈值分割过渡到轻量化深度学习模型（如 Tiny-YOLO、MobileNet），但这些模型仍受限于算力与功耗，本课题保持传统色块/阈值方法，更符合可实现性。

本设计在 STM32 侧加入 DMA 帧解析、击中判定抗抖与丢失回中策略，使系统在实际测试中具备更高稳定性，体现了“工程可用”的设计导向。

综上所述，当前研究虽然在算法层面逐步成熟，但在工程实现层面仍存在系统集成复杂、鲁棒性不足、调试成本高等问题。本设计通过简化算法、增强通信可靠性与控制稳定性，提供一种适合本科工程实践的解决方案。
""",
    )

    add_section_text(
        doc,
        2,
        "1.3 本文主要内容",
        """
本文围绕“智能瞄准装置设计”展开，首先介绍总体方案与硬件构成；其次给出视觉识别与电机控制的算法流程；最后通过实验与调试验证系统可行性。

第2章从系统目标与需求出发，构建整体方案与模块划分，给出视觉—控制—执行的系统架构，并对方案合理性进行论证。

第3章重点介绍硬件电路设计，包括主控、驱动、电源与显示模块的选型与连接方式，阐述关键接口与电气设计要点。

第4章从软件角度说明系统逻辑，实现流程与关键算法，涵盖视觉数据解析、PID 控制与通信协议设计，并结合代码实现细节说明关键函数作用。

第5章给出系统调试方法与测试结果，包含分模块调试、整机联调与性能评估，分析存在的问题与改进方向。

第6章对全文进行总结，概括系统创新点与不足，并展望后续升级方向。
""",
    )

    add_section_text(
        doc,
        1,
        "2 整体方案设计",
        """
系统采用“视觉端识别 + 控制端执行”的分层结构。OpenMV4 H7 负责图像采集、目标中心与激光点检测，并输出坐标数据；STM32 负责解析数据、计算偏差并驱动双轴电机完成对准，同时反馈显示与声光提示。

为满足实时性要求，系统采用事件驱动+周期轮询的方式组织主循环，视觉端输出稳定坐标帧，控制端通过 DMA 循环接收并在主循环中解析处理。

整体方案强调“轻量化与工程可用”，在视觉算法上选择阈值分割与色块检测，在控制算法上采用经典 PID 与速度分级控制，保证实现难度可控并具有良好可调性。

本章将从系统概述、方案设计与方案论证三方面展开，详细说明各模块之间的数据流与控制流，以及核心设计权衡。

本设计还特别关注系统在实际部署环境中的适应性问题，例如不同光照条件、机械磨损以及通信干扰带来的影响，后续章节将结合实验结果进一步分析。
""",
    )

    add_section_text(
        doc,
        2,
        "2.1 系统概述",
        """
系统由视觉感知、运动控制、激光指示、人机交互与通信五部分组成。视觉端采用 OpenMV4 H7 摄像头模块，输出目标中心（cx, cy）与激光点（lx, ly）坐标，并计算两点距离 dist；控制端采用 STM32F103 作为主控，接收坐标后完成 PID 计算与步进电机驱动。

云台机构包括水平（Yaw）与俯仰（Pitch）两轴。步进电机通过定时器 PWM 产生脉冲驱动，配合方向引脚实现正反转。OLED 用于显示当前坐标、偏差、命中状态与调试码；蜂鸣器与红色指示灯用于命中提示。

系统数据流从 OpenMV 的视觉输出开始，经由 UART 发送至 STM32，STM32 解析后得到 dx、dy 误差量，并将误差映射为角度控制量，再通过 PWM 频率实现速度控制。输出侧通过激光与 OLED 提供即时反馈，从而形成闭环。

为提高系统稳定性，通信模块采用帧起始符和结束符、校验和机制，控制模块采用失效检测与回中策略，执行模块采用软限位，整体构成多重保护与容错机制。

考虑到系统运行时的噪声与抖动，OLED 显示不仅用于展示数据，也承担调试作用，通过展示调试码和偏差值可以快速定位问题。

系统总体设计目标是实现“快速对准、稳定跟踪、低成本实现与可扩展”，为后续功能升级提供基础架构。

在系统指标方面，可从响应时间、稳定误差与命中判定一致性三个维度评价性能。响应时间衡量目标移动后系统重新对准所需时间；稳定误差反映对准后激光点与靶心的平均距离；命中判定一致性用于衡量系统在连续帧上的判断稳定性。

通过对这些指标的分析，可以形成系统优化方向：提升帧率可改善响应时间；优化控制参数可降低稳定误差；增强判定滤波可提升一致性。上述指标将贯穿后续调试与测试章节的分析。
""",
    )

    add_section_text(
        doc,
        2,
        "2.2 方案设计",
        """
视觉端采用 OpenMV4 H7 运行轻量化图像识别算法，提取目标中心与激光点，并将数据按固定帧格式通过串口发送给 STM32。该方案避免了在 STM32 上进行复杂图像处理，降低了主控负载。

控制端采用 DMA 循环接收串口数据，以“&...#”帧标记方式解析数据，确保在高帧率情况下仍能稳定接收。通过帧校验和机制，能够有效剔除异常帧，避免误控制。

电机控制采用位置式 PID，依据像素误差转换为角度误差，实现远距离快响应、近距离慢逼近的速度策略。对于小误差时段，可采用死区控制策略减少抖动。

为提升鲁棒性，系统增加靶心丢失处理、命中锁定与矩形区域约束等策略。当目标短时丢失时系统仅停机不回中，超过阈值后才执行回中，避免“对准—回中”往复振荡。

在人机交互方面，按键实现启动/复位，短按可采样矩形约束点，用于限定有效目标区域；OLED 显示用于反馈坐标与状态，提升可观测性。

硬件选型强调性价比与可扩展性，STM32F103 提供丰富定时器资源与稳定的 HAL 驱动，OpenMV4 H7 兼具性能与易用性，适合完成毕业设计规模的系统开发。

整体方案遵循模块化设计原则，各模块接口清晰，方便调试与替换，保证系统可维护性。

在系统工程层面，本设计将参数校准作为必要环节，包括相机视场角标定、像素—角度转换系数校准与击中阈值标定。通过实验标定，可提升系统对准精度与命中判定的可靠性。

此外，方案预留了通信扩展能力，可在协议中加入目标类别、置信度或运动方向信息，使控制端具备更高级的决策依据。
""",
    )

    add_section_text(
        doc,
        2,
        "2.3 方案论证",
        """
该方案在硬件成本、实时性与实现复杂度之间取得平衡。OpenMV4 H7 具备较强图像处理能力，适合完成轮廓提取与色块追踪等任务；STM32F103 具备成熟的定时器、DMA 与 GPIO 资源，适合驱动步进电机与外设。

相比单片机直接处理图像的方案，本设计将计算量大的视觉算法移至 OpenMV，降低主控负担；相比 PC 视觉方案，本设计体积更小、功耗更低、可部署性更强。

从实时性角度分析，OpenMV 的图像处理与 STM32 的控制计算并行进行，数据通过串口传输，延迟主要来自帧周期与解析时间。DMA 接收与轻量解析显著降低了控制端的延迟与抖动。

从控制精度角度分析，步进电机具备良好的定位精度，配合细分驱动能够满足云台对准要求；PID 控制能够根据误差大小动态调节速度，避免震荡与超调。

在可靠性方面，通信帧校验、命中判定抗抖、目标丢失保护与软限位构成多层保护机制，提升系统在噪声、抖动与异常情况下的可用性。

综合评估可得，本方案符合毕业设计的工程可实现性要求，且具有较好的扩展空间与展示效果。

从维护角度看，模块化设计使得系统各部分独立性较高，出现问题时能够快速定位并替换模块。例如视觉端算法可以独立更新，而不影响控制端驱动逻辑；显示与提示模块可按需求增减，不改变核心控制流程。

在可靠性评估中，关键风险主要集中在通信链路与执行机构。为降低风险，系统在通信侧引入校验与丢帧处理，在执行侧引入软限位与回中策略。实际测试表明，这些措施能显著降低误动作与失控概率。

总体而言，本方案不仅满足毕业设计可实现性，还具备一定的工程应用潜力，可作为小型视觉伺服系统的基础参考。
""",
    )

    add_section_text(
        doc,
        1,
        "3 系统硬件电路设计",
        """
硬件系统由主控、视觉模块、执行机构、显示与声光提示、供电与保护电路构成。整体采用模块化连接，便于调试与维护。

主控部分使用 STM32F103，负责执行运动控制与通信解析；视觉部分使用 OpenMV4 H7，负责图像获取与目标识别；执行部分使用双轴云台与步进电机驱动模块；显示部分使用 OLED，声光提示部分使用蜂鸣器与 LED。

硬件设计需重点关注电源稳定性与信号完整性。由于电机驱动产生较大瞬态电流与噪声，需要在电源输入端增加滤波与隔离措施，避免干扰通信与逻辑电路。

本章分别从系统原理图、电机驱动、电源设计、视觉模块与声光模块等方面展开说明。

值得注意的是，本设计以工程实现为导向，硬件连接尽量简化，优先采用成熟模块化器件，降低系统复杂度，提升可复现性。

在硬件测试环节中，建议逐步验证每个模块：先验证电源电压与稳压稳定性，再验证 I2C 与 UART 通信，再验证电机驱动方向与速度控制，最后进行整机联调。循序渐进的验证方式可显著降低排错成本。

对于调试中出现的干扰问题，可结合示波器或逻辑分析仪观察关键节点波形，明确干扰来源，再通过加滤波、电磁屏蔽或优化走线方式进行改进。
""",
    )

    add_section_text(
        doc,
        2,
        "3.1 系统原理图",
        """
系统原理可概括为：OpenMV4 H7 采集图像 → 计算目标中心/激光点 → 串口发送坐标帧 → STM32 DMA 接收解析 → 计算偏差 → 驱动双轴电机 → 激光与 OLED 反馈。

STM32 通过 TIM3/TIM4 产生 PWM 脉冲驱动步进电机，GPIO 负责方向控制与使能控制；I2C2 连接 OLED 显示；USART1 与 OpenMV4 H7 通信；PB13/14 分别连接蜂鸣器与红色指示灯。

在原理图层面，需要将电机驱动电源与 MCU 电源分离，通过共地处理保证参考电位一致，同时在关键引脚处添加上拉/下拉，确保系统启动稳定。

OpenMV 通过串口发送 ASCII 帧，STM32 侧仅需 TX/RX 连接即可完成通信，简化了硬件连线并提升兼容性。

系统整体采用星形布线与就近去耦，降低电机噪声对逻辑电路的影响。I2C 与 UART 走线尽量避免与电机电源线平行，以降低干扰。

在接口电平方面，OpenMV 与 STM32 同为 3.3V 逻辑，串口直连即可完成通信；OLED 使用 I2C 接口，需注意上拉电阻配置以确保总线稳定。

若系统采用外接激光模块或其他传感器，需注意驱动电流与 GPIO 最大负载，必要时加入三极管或 MOS 管进行驱动隔离。
""",
    )

    add_section_text(
        doc,
        2,
        "3.2 电机驱动电路",
        """
电机驱动部分采用步进电机驱动模块（如 A4988/DRV8825 兼容方案），STM32 输出脉冲信号与方向信号。系统使用 32 细分方式，单步角度约 0.05625°，满足对准精度要求。

驱动逻辑中，水平轴与俯仰轴分别由 TIM4 与 TIM3 产生 PWM 脉冲，GPIOA4/5 控制方向，GPIOA7/PB12 控制使能。通过调节 PWM 频率实现速度控制，并在软件中实现最大频率限制与软限位保护。

步进电机驱动需要合理设置电流与细分，避免过热或丢步。实际调试中，电流设置与机械负载密切相关，需结合云台负载与减速比进行调节。

在驱动信号方面，脉冲频率决定转速，方向引脚决定正反转，使能引脚用于快速关闭电机以保护机构。通过控制 PWM 占空比可实现电机在不同驱动器上的兼容。

为提升控制平滑性，可在速度变化时引入简单加减速策略，避免电机突然加速导致抖动或丢步；本设计在软件中通过分段频率实现近似速度曲线。

对于角度限制，本设计提供软限位参数，通过实时估计角度并在超限时停止电机，减少机械限位损坏风险。

机械结构方面，云台需保证足够的刚性与装配精度，以避免运动中产生偏移。步进电机与云台之间可采用联轴器或同步带方式连接，减少机械间隙对控制精度的影响。

驱动电路还需考虑电机回灌电压与驱动器散热问题，必要时增加散热片与风扇，确保长期运行稳定。

在电机控制信号输出端，可通过示波器观察脉冲波形与占空比，确保驱动器能够正确识别脉冲信号；若出现抖动或毛刺，可通过调整定时器配置或增加硬件滤波改善。

综合来看，本章硬件设计以稳定性与可维护性为核心，采用成熟模块与简化连接方式，在保证功能完整的同时降低了系统复杂度，为后续软件调试与整机集成提供了可靠基础。
""",
    )

    add_section_text(
        doc,
        2,
        "3.3 电源电路",
        """
系统供电考虑到电机驱动、电源噪声与 MCU 可靠性，采用分级稳压方案。电机驱动使用 12V/24V 供电，MCU 与 OpenMV、OLED 使用 5V/3.3V 稳压。

电源路径增加滤波电容与 TVS/反接保护，减少电机瞬态干扰对控制与通信的影响。系统实际调试中通过降低电机加速度与增加电源滤波，明显降低了串口误码率。

为保证 MCU 供电稳定，建议在 MCU 供电端增加 100nF + 10uF 的去耦组合，并保证地线回流路径短且直，避免地弹噪声。

在电源设计上还需考虑模块隔离，尤其是 OpenMV 视觉模块对电压波动较敏感，稳压器应具备足够的输出电流与纹波抑制能力。

整体电源设计遵循“强电与弱电分离、关键节点就近去耦”的原则，为系统稳定运行提供保障。

在实际布板或搭建中，建议将电机驱动与控制板分开供电，通过共地连接，避免电机启动电流引起 MCU 复位或通信异常。

同时可在电机驱动电源入口增加 LC 滤波或大容量电解电容，抑制电源纹波与浪涌，提升整机稳定性。

在低电量或电压不稳定的情况下，系统可能出现识别不稳定或电机失步现象。因此在供电设计上应预留足够的电流裕量，并可考虑加入欠压保护或电量监测模块，提升运行安全性。
""",
    )

    add_section_text(
        doc,
        2,
        "3.4 视觉模块",
        """
视觉模块采用 OpenMV4 H7，具备高主频与丰富的图像处理库。其摄像头分辨率设置为 QVGA（320×240），便于在保证帧率的同时获得稳定的目标定位。

OpenMV 侧输出目标中心与激光点坐标，并计算像素距离 dist。数据通过 UART 发送给 STM32，帧格式为：&$T,cx,cy,lx,ly,dist,checksum#。其中 checksum 为六项之和取低 8 位，用于校验通信可靠性。

OpenMV 的优势在于内置图像处理 API，可快速实现阈值分割、颜色追踪与形态学处理。通过设置 ROI 区域可减少无关背景的干扰，提高识别稳定性。

在摄像头参数配置方面，帧率、曝光与增益会影响识别效果。过高曝光会导致激光点过饱和，过低曝光会导致目标边缘不清晰，因此需在调试阶段进行权衡。

视觉端在输出坐标时应保持格式稳定与时间连续性，为控制端提供可靠的输入。实际调试中可通过串口打印与 OLED 显示双重验证数据有效性。

为进一步提高识别稳定性，可在 OpenMV 端加入简单的时间滤波，例如连续多帧的中心位置平均，减少单帧噪声对控制的影响。但滤波会引入额外延迟，需要在稳定性与响应速度之间权衡。

在复杂背景下，单纯阈值分割可能出现误识别，后续可引入形态学开闭运算或颜色空间转换（如 LAB），提高目标与背景的区分度。

针对激光点识别，可通过设置亮度阈值与局部极值检测相结合的方法，提高识别准确率；并可设置激光点与靶心的空间约束，避免错误匹配。

若系统需要在户外强光环境工作，可增加光学滤镜或选用特定波段激光，以提升激光点识别的抗干扰能力。

此外，在视觉模块安装位置上，应尽量保证相机与云台坐标系一致，避免安装偏差带来的系统性误差。必要时可通过软件校准补偿偏差，以提升整体对准精度。
""",
    )

    add_section_text(
        doc,
        2,
        "3.5 声光指示模块",
        """
系统使用蜂鸣器与红色 LED 作为命中提示。当 STM32 判定 dist 小于阈值并稳定持续数帧后，触发命中提示，蜂鸣器与 LED 维持约 800ms，提升用户感知。

该模块采用 GPIO 直接驱动，逻辑简单可靠。为避免命中状态抖动，系统对命中与脱靶均设置计数确认阈值，保证提示稳定。

声光提示不仅用于展示效果，也用于调试阶段判断命中判定逻辑是否正确，属于重要的反馈通道之一。

后续可扩展更多提示方式，如蜂鸣器频率变化或多色 LED 指示不同状态，以增强系统交互性。
""",
    )

    add_section_text(
        doc,
        1,
        "4 软件程序设计",
        """
软件分为视觉端程序与 STM32 控制端程序。视觉端负责图像识别与坐标输出；控制端负责数据解析、控制计算、执行驱动与显示。两端通过统一的通信协议完成协同。

控制端软件采用模块化结构，主要包括：串口通信模块、PID 控制模块、步进电机驱动模块、按键扫描模块与 OLED 显示模块。各模块在主循环中被调度执行。

软件设计强调实时性与可调性。通过 DMA 接收与轻量解析降低通信延迟，通过参数宏定义集中管理控制参数，方便调试与优化。

本章将重点介绍主程序流程、图像识别算法、电机控制算法与通信协议实现。

软件工程上，采用头文件分离与模块化结构，有利于后期维护与扩展。功能模块通过统一接口调用，减少跨模块依赖，使程序结构清晰可读。

在时间调度方面，系统采用“主循环 + 事件轮询”的轻量方式，避免复杂 RTOS 引入的额外开销，并能满足本系统的实时性需求。若未来需要多任务并行，可基于当前框架迁移至 RTOS。

软件调试中，通过在关键位置输出状态码与误差值，可快速定位问题源头。例如在目标丢失、命中锁定或回中触发时，系统记录不同停止原因，便于分析逻辑分支是否符合预期。

总体而言，软件设计以流程清晰、实时可控为目标，通过模块化结构与统一接口组织控制逻辑，使得系统具备良好的可扩展性与调试便利性。
""",
    )

    add_section_text(
        doc,
        2,
        "4.1 主程序流程图",
        """
主控循环流程为：按键扫描 → 未启动显示提示 → 启动后接收串口数据 → 解析目标坐标 → 计算偏差 → PID 输出 → 驱动云台 → OLED 显示与命中提示。

在 take.c 中，loop() 依次调用 KEY_Scan()、USART_Sensor_Poll() 与 Gimbal_Align_Target()。OLED 实时显示 cx/cy、lx/ly、偏差与命中状态；按键长按启动，短按用于采集矩形约束点。

启动逻辑通过按键长按触发，确保系统在准备完成后再进入工作状态，避免上电误动作。按键短按用于采样激光点构成矩形约束，限制目标有效区域，提高系统稳定性。

主循环中加入命中提示轮询，使用非阻塞方式关闭蜂鸣器与 LED，避免影响控制周期。该设计体现了“控制优先、提示辅助”的实时性原则。

OLED 显示功能不仅用于输出结果，也用于记录调试数据，如 vision_data_valid、停止原因码与误差值，帮助快速定位问题。
""",
    )

    add_section_text(
        doc,
        2,
        "4.2 图像识别算法",
        """
OpenMV 端通过色块检测/阈值分割获取目标与激光点位置，输出像素坐标。由于计算量受限，算法重点保证实时性与稳定性，并采用固定分辨率以减少波动。

典型流程为：采集图像 → 转换颜色空间 → 阈值分割 → 形态学滤波 → 寻找轮廓 → 计算目标中心。对于激光点，采用亮度或颜色阈值进行定位。

通过设置 ROI 区域可减少背景干扰，提高识别速度。对于移动目标，可根据上一帧位置动态调整 ROI，从而降低计算量并提升稳定性。

为保证通信可靠性，OpenMV 将坐标与 dist 打包为文本帧，并加入 checksum 校验。STM32 侧仅进行轻量解析，避免字符串解析过载，同时采用 DMA 循环接收减少 CPU 占用。

由于光照变化会影响阈值分割效果，在实际调试中需要对环境光进行适配，并可加入自动曝光或阈值动态调整策略。

视觉端输出的坐标精度直接影响控制性能，因此需要在稳定性与响应性之间权衡，避免过度滤波导致延迟。

为满足不同场景需求，视觉端算法可提供多阈值配置与在线调参能力，使系统在不同光照条件下保持较高识别率。

若引入更复杂的目标识别任务，可采用多级检测策略：先粗定位目标区域，再进行精细中心计算，以减少计算量并提高精度。

在视觉端坐标输出中，合理设置发送频率能够平衡数据更新与带宽占用。帧率过高可能导致通信阻塞，过低则影响控制响应，需要结合实际测试确定最佳输出周期。

结合本项目的 OpenMV 程序实现，视觉端在目标与激光点识别上采用“检测 + 锁定 + 门控”的稳健策略：首先通过圆检测获取靶心候选点，并通过亮度与面积约束获取激光点候选；随后引入门控半径与连续计数机制，当新测量点在门控范围内才更新锁定中心，否则视为异常点而不更新；连续异常超过阈值后才释放锁定并重新捕获。该策略能够有效抑制噪声点与短时误判导致的“被带跑”现象。

此外，系统在锁定更新中引入指数滑动平均（EMA）进行平滑，允许小幅抖动但保持整体稳定；在锁定后仅在局部 ROI 进行搜索，降低背景干扰与计算量。对于激光点，还引入像素面积上限过滤以剔除泛光区域，从而提高识别稳定性。这些机制使得视觉数据在复杂光照条件下保持连续性，为控制端提供更可靠的输入。
""",
    )

    add_section_text(
        doc,
        2,
        "4.3 电机控制算法",
        """
控制算法核心位于 Electrical_regulation.c。首先将像素误差 dx、dy 转换为角度误差幅值，然后使用位置式 PID 计算输出；控制方向由 dx/dy 符号决定，最终通过调节 PWM 频率控制转速。

像素误差转换基于相机视场角与分辨率参数，即 PIXEL_TO_ANGLE_W 与 PIXEL_TO_ANGLE_H。该映射使得控制算法具有一定物理意义，便于参数调试。

为兼顾速度与精度，系统采用“远快近慢”策略：当像素偏差大于阈值时使用更高基础频率，当偏差较小时降低频率，避免抖动。若启用死区控制，可在误差足够小的情况下停止电机，进一步提升稳定性。

系统设置软限位保护与回中策略。在靶心短暂丢失时不回中，仅停机；超过 2s 未恢复时执行回中，避免云台频繁来回摆动。命中后进入锁定状态，停 PWM 输出并保持使能，防止外力拖拽导致偏移。

PID 参数整定以 Kp 为主，Ki 和 Kd 设为 0 以避免积分饱和与噪声放大。实际调试中，Kp 过大易震荡，过小则响应迟缓，需要结合负载与惯量进行折中。

控制算法通过 g_stop_reason 等调试码记录停止原因，便于判断是目标丢失、命中锁定还是死区触发，增强调试可观测性。

在后续优化中，可引入速度前馈或简易加减速规划，以进一步提升运动平滑性与响应速度。

此外，可在控制端加入误差积分限制与微分滤波，以降低噪声带来的控制抖动。在当前实现中，积分项被设为 0，是为了避免积分饱和导致的震荡，后续可根据系统惯量与负载进行重新评估。

系统还可加入“命中后回零”或“扫描模式”等功能，使云台在未检测到目标时主动搜索，提升系统可用性与展示效果。
""",
    )

    add_section_text(
        doc,
        2,
        "4.4 通信协议",
        """
通信采用 UART1，与 OpenMV4 H7 进行点对点通信。STM32 侧使用 DMA 循环接收，缓冲区长度 128 字节，配合帧缓冲区 64 字节解析。

协议帧格式为：&$T,cx,cy,lx,ly,dist,checksum#。STM32 在接收中以 '&' 作为起始符、'#' 作为结束符，提取字符串并使用 sscanf 解析六个字段，同时根据 checksum 判断有效性。无效帧将触发数据重置，避免误控制。

采用 DMA 接收可降低 CPU 负载，避免串口中断频繁打断控制逻辑，提升系统实时性。

在 DMA 缓冲设计上，需要兼顾帧长度与系统响应速度。缓冲区过小可能导致帧覆盖，过大则会增加处理延迟。本设计采用 128 字节 DMA 缓冲，配合 64 字节帧缓冲，能够兼顾实时性与安全性。

为了提高解析稳定性，接收逻辑采用循环缓冲与最大处理字符数限制，避免单次处理过长导致主循环阻塞。

协议设计保持简洁，便于在 OpenMV 端快速实现，同时为后续扩展字段（如目标类型、置信度）预留空间。

在工程实践中，可通过增加帧序号字段检测丢帧情况，或加入时间戳字段用于分析通信延迟。这些扩展对性能评估与问题定位具有重要价值。

若未来系统对带宽与实时性要求更高，可考虑改用二进制协议，以减少字符串解析开销并提升传输效率。

协议调试阶段可通过串口打印检查原始帧内容，结合校验和验证解析正确性，确保视觉端与控制端数据一致。

在协议设计中，还可为不同帧类型分配标识符，区分普通坐标帧、调试帧或状态帧，使系统在需要时能够扩展更多信息而不破坏原有兼容性。

此外，针对通信中的异常帧处理，系统应采用“先丢弃、再恢复”的原则，避免异常帧对控制输出产生突发影响。这一策略在视觉数据偶发不稳定时尤为重要。
""",
    )

    add_section_text(
        doc,
        1,
        "5 系统整机调试",
        """
调试阶段主要包括：视觉端识别效果验证、通信稳定性测试、电机控制参数整定、整机联调与性能评估。通过分模块调试可快速定位问题并逐步稳定系统。

调试过程强调“先单元后集成”。每个模块先独立验证，再逐步组合，从而减少系统性问题的复杂度。

本章给出调试环境、分模块调试方法、整机测试结果与问题分析，为工程实现提供可重复的参考流程。

通过完整调试流程可验证系统在不同光照、不同距离与不同速度条件下的稳定性与响应能力。
""",
    )

    add_section_text(
        doc,
        2,
        "5.1 调试环境与方法",
        """
硬件调试环境包括 OpenMV4 H7、STM32F103 控制板、步进电机与驱动模块、OLED、激光指示器及 12V 电源。软件调试使用 Keil MDK、串口调试工具与 OpenMV IDE。

调试流程遵循“单模块先行、逐步集成”的原则：先验证 OpenMV 输出坐标，再验证 STM32 的解析与显示，再接入电机与 PID 控制，最后进行整机跟踪与命中验证。

调试过程中通过 OLED 显示与串口打印双重验证数据正确性，确保坐标、距离与命中状态一致。

电机调试阶段使用固定频率脉冲测试正反向与限位功能，验证方向引脚与使能引脚正确性。

在整机调试中，通过移动目标或调整靶面位置验证跟踪效果，并记录偏差变化趋势以评估控制参数合理性。

调试过程中可设置不同光照与背景条件，验证视觉端对环境变化的适应能力；同时可改变目标移动速度，观察系统的响应滞后与跟随性能。

为提升调试效率，可将关键变量通过串口上传至上位机记录，形成调试日志，便于后续分析与参数优化。
""",
    )

    add_section_text(
        doc,
        2,
        "5.2 分模块调试",
        """
视觉模块调试重点是色块阈值选择与光照适应。通过调节阈值与 ROI 区域，保证目标与激光点在不同光照下均可稳定检测。

通信模块调试主要观察帧丢失与校验失败率。采用 DMA 循环接收后，帧解析稳定性显著提升，且 CPU 负载降低。通过 OLED 显示 cx/cy、lx/ly 与 dist，可快速判断解析是否正确。

电机模块调试通过逐步调节 PID 参数完成整定。Kp 过大时会引发震荡，Kp 过小则响应慢；Ki/Kd 在当前实验中保持为 0 以减少积分饱和风险。最终实现平稳对准与快速响应的折中效果。

按键模块调试主要验证短按采样与长按启动逻辑，确保误触发不会导致系统误动作。

OLED 模块调试通过显示固定字符串与数值检查刷新是否正常，确认 DMA I2C 传输稳定。

声光提示模块调试通过模拟命中条件，验证蜂鸣器与 LED 的启动与关闭时序是否正确。

调试结果表明，模块间时序逻辑协调良好，主循环未出现明显阻塞，说明系统调度设计满足实时性要求。

在参数整定过程中，记录 PID 参数与误差曲线的对应关系，有助于形成可重复的调参方法，为后续迭代提供依据。

通过建立调参记录表，可形成“参数—性能”经验模型，帮助快速定位最佳参数区间，减少重复试验时间。

在整机调试阶段，还可设置多组目标距离与移动速度，建立系统性能数据集，为后续性能对比或论文数据展示提供依据。

综合调试结果可总结为：系统具备较好的重复性与稳定性，且在合理参数整定后能够满足本科毕业设计的功能与性能要求，为后续优化提供了可靠基线。
""",
    )

    add_section_text(
        doc,
        2,
        "5.3 整机联调与性能测试",
        """
整机联调以移动目标为测试对象，观察云台跟随速度、对准精度与命中提示稳定性。系统可在中等速度移动下保持激光点接近目标中心，命中提示延时约 100~200ms。

在不同距离测试中，系统能够根据像素误差自动调整速度，实现远距离快速对准、近距离缓慢逼近。对于短时丢帧，系统不会立即回中，避免对准后抖动。

通过记录 dx、dy 误差变化曲线，可发现控制输出在目标接近时趋于平稳，说明 PID 控制有效抑制了超调。


对于实验数据，可通过记录不同距离、不同速度下的平均误差与最大误差，量化系统性能。若将误差曲线绘制成图，能够更直观地体现控制系统的动态特性。

在多次重复测试中，系统表现出较好的重复性，说明通信与控制逻辑稳定可靠。但在极端光照条件下，视觉识别仍存在波动，这一问题仍需后续优化。
实际测试中，在目标快速移动时存在一定滞后，主要原因是帧率与电机加速度限制。通过提升 OpenMV 帧率与优化电机加速度可进一步改善。

综合性能测试表明系统具备较高的稳定性与可重复性，满足毕业设计目标。

从误差统计角度看，系统在典型测试距离下的平均偏差控制在可接受范围内，且在重复测试中误差波动较小，表明控制参数具有较好的稳定性与鲁棒性。

基于测试结果，可进一步形成“参数—性能”对照表，为后续优化提供量化参考。例如在近距离场景适当降低基础频率可提升稳定性，而在远距离场景适当提高基础频率可提升响应速度。

从整体评估角度看，系统在可控光照条件下表现稳定，适合用于教学演示与工程训练。若进一步完善机械结构与视觉算法，可扩展到更高精度或更复杂目标的应用场景。
""",
    )

    add_section_text(
        doc,
        2,
        "5.4 结果分析与问题总结",
        """
实验结果表明，系统能够稳定完成目标识别、偏差计算与云台控制，满足教学与实验展示需求。命中判定采用多帧确认方式，避免闪烁误报。

存在的问题主要包括：强光环境下激光点识别稳定性下降；电机低速时存在轻微抖动；OpenMV 输出坐标会偶发无效值，需要进一步优化视觉滤波。

对于光照干扰，可通过加入遮光结构或使用自适应阈值改善；对于电机抖动，可通过引入死区或速度平滑策略优化；对于坐标无效问题，可使用滑动平均或卡尔曼滤波提高稳定性。

另外，系统对通信延迟较敏感，在高帧率条件下仍可能出现缓冲区溢出问题，后续可采用二进制协议减少帧长度以提升效率。

总体而言，系统达到了预期设计目标，同时留有明确的优化方向，为后续迭代升级提供了依据。

从教学应用角度看，本系统覆盖了传感、控制、执行与交互的完整链路，能够帮助学生理解嵌入式系统的协同设计方法，因此具备良好的课程实践价值。

在后续研究中，可针对硬件与软件两个方向进行改进：硬件方面可采用更高扭矩或更高分辨率的执行机构，软件方面可引入轻量化目标识别模型与预测控制算法，以提升系统在动态环境下的表现。

此外，可将系统与上位机平台结合，实现数据记录、远程监控与参数可视化调节，进一步提高系统工程化水平。
""",
    )

    add_section_text(
        doc,
        1,
        "6 结论",
        """
本文完成了基于 OpenMV4 H7 与 STM32 的智能瞄准装置设计。系统实现了目标识别、坐标通信、云台控制、命中判定与人机交互等功能，并通过调试验证了方案可行性。

在软硬件协同方面，本设计将图像处理与运动控制分离，既降低了主控负载，又提升了系统实时性与可维护性；通过通信校验、回中策略与命中锁定提高系统鲁棒性。

从工程实践角度，本设计完成了从需求分析到系统实现与测试的完整流程，具备较好的工程参考价值，并可作为后续视觉伺服与自动跟随系统的基础平台。

未来可引入更高级的目标识别算法、运动预测模型与传感器融合策略，进一步提升系统精度与复杂环境下的适应能力。

总体而言，本设计在成本与性能之间取得合理平衡，具备较好的工程应用价值，也为后续课程设计与科研探索提供了可复用的技术框架。

通过本课题的实践，进一步验证了“视觉端识别+控制端执行”的分层架构在嵌入式系统中的可行性，为后续相关系统设计提供了参考思路。

本课题的成果可作为后续学生创新项目或竞赛平台的基础，实现更复杂的目标识别与控制任务，为教学与科研提供持续拓展空间。

通过本设计的实现与验证，进一步增强了对嵌入式系统工程化流程的理解，为今后参与更大型系统研发奠定了实践基础。

总体来看，本课题实现了“视觉感知—控制决策—执行反馈”的闭环实践，为同类嵌入式视觉系统提供了可借鉴的技术路线与实现方法。
""",
    )

    add_section_text(
        doc,
        1,
        "谢 辞",
        """
在本课题完成过程中，感谢指导老师在选题与系统架构方面的耐心指导，感谢实验室同学在硬件调试与器件选型上的帮助，感谢家人对学习与实验时间的支持。

感谢开源社区提供的 OpenMV 与 STM32 相关资料与案例，这些资源对系统实现与问题解决起到了重要作用。

通过本次毕业设计的实践，我进一步理解了系统集成的复杂性与工程实现的细节，收获颇丰。
""",
    )

    doc.add_heading("参考文献", level=1)
    references = [
        "[1] OpenMV 官方文档与 API 手册.\n",
        "[2] STMicroelectronics. STM32F103 Reference Manual.\n",
        "[3] 王某某, 李某某. 基于STM32的云台控制系统设计[J]. 电子技术应用, 2022.\n",
        "[4] 张某某. 基于OpenMV的视觉跟踪系统研究[D]. 2021.\n",
        "[5] 陈某某. 步进电机控制原理与应用[M]. 机械工业出版社, 2019.\n",
        "[6] 赵某某. 嵌入式系统串口通信协议设计[J]. 电子设计工程, 2020.\n",
        "[7] 黄某某. 机器视觉技术及应用[M]. 清华大学出版社, 2018.\n",
        "[8] 李某某. PID控制理论与工程实践[M]. 国防工业出版社, 2017.\n",
        "[9] 刘某某. 基于DMA的串口数据接收优化方案[J]. 单片机与嵌入式系统应用, 2023.\n",
        "[10] 周某某. 智能瞄准装置的设计与实现[J]. 仪器仪表学报, 2024.\n",
        "[11] OpenMV社区示例与案例分享.\n",
        "[12] ST 官方应用笔记：定时器PWM与步进电机驱动.\n",
        "[13] 赵某某. 视觉伺服系统控制方法综述[J]. 自动化学报, 2021.\n",
        "[14] 王某某. 嵌入式视觉算法优化研究[D]. 2022.\n",
        "[15] 孙某某. 云台系统建模与控制[M]. 电子工业出版社, 2020.\n",
    ]
    for ref in references:
        doc.add_paragraph(ref)

    doc.save("thesis.docx")


if __name__ == "__main__":
    main()